server:
  port: 8082

spring:
  application:
    name: student-microservice
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    appname: Student MicroService
